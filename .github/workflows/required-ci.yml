name: required-ci
on: [push, pull_request]
jobs:
  required-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject unsafe changes
        run: |
          echo "CI FAILED - this change must not enter the queue"
          exit 1
